(define (cs) (error "no turtle graphics"))
(define (fd) (error "no turtle graphics"))
(define (bk) (error "no turtle graphics"))
(define (lt) (error "no turtle graphics"))
(define (rt) (error "no turtle graphics"))
(define (setxy) (error "no turtle graphics"))
(define (xcor) (error "no turtle graphics"))
(define (ycor) (error "no turtle graphics"))
(define (pos) (error "no turtle graphics"))
(define (setheading) (error "no turtle graphics"))
(define (heading) (error "no turtle graphics"))
(define (st) (error "no turtle graphics"))
(define (ht) (error "no turtle graphics"))
(define (shown?) (error "no turtle graphics"))
(define (pendown) (error "no turtle graphics"))
(define (penup) (error "no turtle graphics"))
(define (penerase) (error "no turtle graphics"))
(define (home) (error "no turtle graphics"))
(define (pc) (error "no turtle graphics"))
(define (setpc) (error "no turtle graphics"))
(define (setbg) (error "no turtle graphics"))
